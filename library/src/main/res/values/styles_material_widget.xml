<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="HD_MaterialButton" parent="Widget.MaterialComponents.Button">
        <!-- 主题扩展：Widget.MaterialComponents.Button -->
        <!-- 主题扩展：Widget.MaterialComponents.Button.TextButton -->
        <!-- 主题扩展：Widget.MaterialComponents.Button.OutlinedButton -->
        <!-- 主题扩展：Widget.MaterialComponents.Button.UnelevatedButton -->

        <!-- 最小宽度（android:minWidth，默认：88dp） -->
        <!-- 最小高度（android:minHeight，默认：48dp） -->

        <!-- 内间距（android:padding，默认：无） -->
        <!-- 内间距（android:paddingLeft，默认：16dp） -->
        <!-- 内间距（android:paddingRight，默认：16dp） -->
        <!-- 内间距（android:paddingStart，默认：16dp） -->
        <!-- 内间距（android:paddingEnd，默认：16dp） -->
        <!-- 内间距（android:paddingTop，默认：4dp） -->
        <!-- 内间距（android:paddingBottom，默认：4dp） -->

        <!-- 阴影间距（android:insetLeft，默认：0dp） -->
        <!-- 阴影间距（android:insetRight，默认：0dp） -->
        <!-- 阴影间距（android:insetTop，默认：6dp） -->
        <!-- 阴影间距（android:insetBottom，默认：6dp） -->

        <!-- 背景颜色（app:backgroundTint，默认：?attr/colorPrimary） -->
        <!-- 背景颜色（app:backgroundTintMode，默认：Mode.SRC_IN） -->

        <!-- 图标（app:icon，默认：无） -->
        <!-- 图标颜色（app:iconTint，默认：?attr/colorOnPrimary） -->
        <!-- 图标大小（app:iconSize，默认：无） -->
        <!-- 图标位置（app:iconGravity，默认：无） -->
        <!-- 图标内间距（app:iconPadding，默认：8dp） -->

        <!-- 边框颜色（app:strokeColor，默认：?attr/colorOnSurface） -->
        <!-- 边框大小（app:strokeWidth，默认：无） -->

        <!-- 文本字体（android:fontFamily，默认：sans-serif-medium） -->
        <!-- 文本位置（android:gravity，默认：center） -->
        <!-- 文本大小（android:textSize，默认：14sp） -->
        <!-- 文本颜色（android:textColor，默认：?attr/colorPrimary 或 ?attr/colorOnPrimary） -->
        <!-- 英文强制大写（android:textAllCaps，默认：true） -->

        <!-- 圆角大小（cornerRadius，默认：4dp） -->
        <!-- 水波纹颜色（app:rippleColor，默认：?attr/colorOnPrimary） -->
        <!-- 取消阴影（android:stateListAnimator=@null，app:elevation=0dp，默认：无） -->
    </style>

    <style name="HD_MaterialButton_Normal" parent="HD_MaterialButton">
        <item name="android:minWidth">@dimen/hd_dp88</item>
        <item name="android:minHeight">@dimen/hd_dp48</item>
        <item name="android:paddingLeft">@dimen/hd_dp16</item>
        <item name="android:paddingRight">@dimen/hd_dp16</item>
        <item name="android:paddingStart">@dimen/hd_dp16</item>
        <item name="android:paddingEnd">@dimen/hd_dp16</item>
        <item name="android:paddingTop">@dimen/hd_dp4</item>
        <item name="android:paddingBottom">@dimen/hd_dp4</item>
        <item name="android:insetTop">@dimen/hd_dp6</item>
        <item name="android:insetBottom">@dimen/hd_dp6</item>
        <item name="cornerRadius">@dimen/hd_dp4</item>
        <item name="iconPadding">@dimen/hd_dp8</item>
        <item name="android:fontFamily">sans-serif</item>
        <item name="android:textSize">@dimen/google_font_medium</item>
        <item name="android:textAllCaps">false</item>
    </style>

    <style name="HD_MaterialButton_Unelevated" parent="HD_MaterialButton_Normal">
        <item name="android:insetTop">@dimen/hd_dp0</item>
        <item name="android:insetBottom">@dimen/hd_dp0</item>
        <item name="android:stateListAnimator">@null</item>
        <item name="elevation">@dimen/hd_dp0</item>
    </style>

    <style name="HD_MaterialCheckBox" parent="Widget.MaterialComponents.CompoundButton.CheckBox">
        <!-- 最小宽度（android:minWidth，默认：48dp） -->
        <!-- 最小高度（android:minHeight，默认：48dp） -->

        <!-- 选框大小（通过android:scaleX和android:scaleY，设置缩放倍数调整大小。不建议修改） -->
        <!-- 选框与控件边缘的距离（android:padding，默认：6dp） -->

        <!-- 选中时边框颜色（enable=true时： ?attr/colorControlActivated，enable=false时：根据主题的colorSurface和colorOnSurface计算取值） -->
        <!-- 未选中时边框颜色（根据主题的colorSurface和colorOnSurface计算取值，默认：灰色。不建议修改） -->

        <!-- 选中时背景颜色（默认：?attr/colorControlActivated） -->
        <!-- 未选中时背景颜色（默认：透明色） -->

        <!-- 文本字体（android:fontFamily，默认：sans-serif） -->
        <!-- 文本与选框位置（android:gravity，默认：center） -->
        <!-- 文字与选框的相对位置（android:layoutDirection，默认：ltr） -->
        <!-- 文本大小（android:textSize，默认：14sp） -->
        <!-- 文本颜色（android:textColor，默认：?attr/textColorPrimaryDisableOnly） -->
        <!-- 英文强制大写（android:textAllCaps，默认：false） -->

        <!-- 统一颜色（android:buttonTint，默认：无。设置后只会影响边框和选中后的背景色，不影响水波纹颜色。不建议使用） -->
        <!-- 水波纹颜色（默认：选中时：0.1透明度的?attr/colorControlActivated，未选中时：?attr/colorControlHighlight。若要取消水波纹，设置android:background为透明色即可） -->
    </style>

    <style name="HD_MaterialCheckBox_Normal" parent="HD_MaterialCheckBox">
        <item name="android:minWidth">@dimen/hd_dp23</item>
        <item name="android:minHeight">@dimen/hd_dp23</item>
        <item name="android:paddingEnd">@dimen/hd_dp6</item>
        <item name="android:paddingRight">@dimen/hd_dp6</item>
        <item name="android:textSize">@dimen/hd_sp14</item>
    </style>

    <style name="HD_MaterialRadioButton" parent="Widget.MaterialComponents.CompoundButton.RadioButton">
        <!-- 最小宽度（android:minWidth，默认：48dp） -->
        <!-- 最小高度（android:minHeight，默认：48dp） -->

        <!-- 选框大小（通过android:scaleX和android:scaleY，设置缩放倍数调整大小。不建议修改） -->
        <!-- 选框与控件边缘的距离（android:padding，默认：6dp） -->

        <!-- 选中时边框颜色（enable=true时： ?attr/colorControlActivated，enable=false时：根据主题的colorSurface和colorOnSurface计算取值） -->
        <!-- 未选中时边框颜色（根据主题的colorSurface和colorOnSurface计算取值，默认：灰色。不建议修改） -->

        <!-- 选中时背景颜色（默认：?attr/colorControlActivated） -->
        <!-- 未选中时背景颜色（默认：透明色） -->

        <!-- 文本字体（android:fontFamily，默认：sans-serif） -->
        <!-- 文本与选框位置（android:gravity，默认：center） -->
        <!-- 文字与选框的相对位置（android:layoutDirection，默认：ltr） -->
        <!-- 文本大小（android:textSize，默认：14sp） -->
        <!-- 文本颜色（android:textColor，默认：?attr/textColorPrimaryDisableOnly） -->
        <!-- 英文强制大写（android:textAllCaps，默认：false） -->

        <!-- 统一颜色（android:buttonTint，默认：无。设置后只会影响边框和选中后的背景色，不影响水波纹颜色。不建议使用） -->
        <!-- 水波纹颜色（默认：选中时：0.1透明度的?attr/colorControlActivated，未选中时：?attr/colorControlHighlight。若要取消水波纹，设置android:background为透明色即可） -->
    </style>

    <style name="HD_MaterialRadioButton_Normal" parent="HD_MaterialRadioButton">
        <item name="android:minWidth">@dimen/hd_dp23</item>
        <item name="android:minHeight">@dimen/hd_dp23</item>
        <item name="android:paddingEnd">@dimen/hd_dp6</item>
        <item name="android:paddingRight">@dimen/hd_dp6</item>
        <item name="android:textSize">@dimen/hd_sp14</item>
    </style>

    <style name="HD_MaterialSwitch" parent="Widget.MaterialComponents.CompoundButton.Switch">
        <!-- 最小宽度（android:minWidth，默认：48dp） -->
        <!-- 最小高度（android:minHeight，默认：48dp） -->

        <!-- 选框最小宽度（android:switchMinWidth，默认：无） -->
        <!-- 选框大小（通过android:scaleX和android:scaleY，设置缩放倍数调整大小。不建议修改） -->
        <!-- 选框与控件边缘的距离（android:padding，默认：6dp） -->

        <!-- 选中时轨道颜色（根据主题的colorSurface和colorOnSurface计算取值。不建议修改） -->
        <!-- 未选中时轨道颜色（根据主题的colorSurface和colorOnSurface计算取值。不建议修改） -->

        <!-- 选中时按钮颜色（根据主题的colorControlActivated计算取值，默认：?attr/colorControlActivated） -->
        <!-- 未选中时按钮颜色（根据主题的colorSurface计算取值。不建议修改） -->

        <!-- 按钮文字是否显示（showText，默认：false） -->
        <!-- 按钮文字样式（switchTextAppearance，默认：@style/TextAppearance.AppCompat.Widget.Switch） -->
        <!-- 按钮文字打开时内容（android:textOn，默认：@string/abc_capital_on） -->
        <!-- 按钮文字关闭时内容（android:textOff，默认：@string/abc_capital_off） -->

        <!-- 文本字体（android:fontFamily，默认：sans-serif） -->
        <!-- 文本与选框位置（android:gravity，默认：center） -->
        <!-- 文本与选框间距（app:switchPadding，默认：0dp） -->
        <!-- 文字与选框的相对位置（android:layoutDirection，默认：rtl） -->
        <!-- 文本大小（android:textSize，默认：14sp） -->
        <!-- 文本颜色（android:textColor，默认：?attr/textColorPrimaryDisableOnly） -->
        <!-- 英文强制大写（android:textAllCaps，默认：false） -->

        <!-- 水波纹颜色（默认：选中时：0.1透明度的?attr/colorControlActivated，未选中时：?attr/colorControlHighlight。若要取消水波纹，设置android:background为透明色即可） -->
    </style>

    <style name="HD_MaterialSwitch_Normal" parent="HD_MaterialSwitch">
        <item name="android:minWidth">@dimen/hd_dp23</item>
        <item name="android:minHeight">@dimen/hd_dp23</item>
        <item name="switchPadding">@dimen/hd_dp6</item>
        <item name="android:padding">@dimen/hd_dp6</item>
        <item name="android:layoutDirection">rtl</item>
        <item name="android:textSize">@dimen/hd_sp14</item>
    </style>
</resources>